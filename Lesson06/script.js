/**
 * Построение кривых Безье
 * P0`` - точка через которую проходит кривая Безье
 * p0` = p0 * (1-t) + p1 * t
 * p1` = p1 * (1-t) + p2 * t
 * p0`` = p0` * (1-t) + p1` * t
 *
 * function draw_curve(P0, P1, P2)
 *  // Если расстояние от точки P1 до отрезка P1P2 меньше 1, то рисуем отрезок, иначе
 *  P0` = (p0 + p1) * 0.5
 *  P1` = (p1 + p2) * 0.5
 *  P0`` = (P0` + P1`) * 0.5
 *  draw_curve(P0, P0`, P0``)
 *  draw_curve(P0``, P1`, P2)
 **/